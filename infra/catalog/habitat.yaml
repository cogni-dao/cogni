name: habitat
type: node
port: 3200
node_port: 31200
dockerfile: nodes/habitat/app/Dockerfile
image_tag_suffix: "-habitat"
migrator_tag_suffix: "-habitat-migrate"
source_repo: https://github.com/Cogni-DAO/habitat.git
image_repository: ghcr.io/cogni-dao/habitat
# Accepted deploy SHA — catalog pin replacing the gitlink (CATALOG_SOURCE_SHA_IS_THE_DEPLOY_PIN).
source_sha: b9457fbc328222b52e65160d6ca8b665a5a69416
candidate_a_branch: deploy/candidate-a-habitat
preview_branch: deploy/preview-habitat
production_branch: deploy/production-habitat
# task.5017 — per-env node-set (deploy ⊆ provisioned). A wizard birth enters all
# NODE_FORMATION_ENVS; this list is the catalog twin so render-node-appset.sh
# renders exactly the births appset.ts generates. Trimming a node's reach is a
# later catalog edit (drop an env here) once its envs are provisioned.
envs: [candidate-a, preview, production]
path_prefix: nodes/habitat/
node_id: dbf1eeb7-85d4-4fd5-a4fe-da85c668bb03
