name: resy
type: node
port: 3000
node_id: "f6d2a17d-b7f6-4ad1-a86b-f0ad2380999e"
dockerfile: nodes/resy/app/Dockerfile
# bug.0344 — see infra/catalog/operator.yaml for why these fields exist.
image_tag_suffix: "-resy"
migrator_tag_suffix: "-resy-migrate"
# task.0320 — per-env deploy branches (see operator.yaml).
candidate_a_branch: deploy/candidate-a-resy
preview_branch: deploy/preview-resy
production_branch: deploy/production-resy
path_prefix: nodes/resy/

# bug.5002 — per-env public URL. Verify scripts read this directly
# instead of computing "${node}-${DOMAIN}" with an "if node = operator"
# special-case + 3-dot heuristic. Catalog is SSoT per ci-cd.md axiom 16.
public_url:
  candidate-a: https://resy-test.cognidao.org
  preview: https://resy-preview.cognidao.org
  production: https://resy.cognidao.org
