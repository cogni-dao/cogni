name: red
type: node
port: 3200
node_port: 31400
dockerfile: nodes/red/app/Dockerfile
image_tag_suffix: "-red"
migrator_tag_suffix: "-red-migrate"
source_repo: https://github.com/cogni-dao/red.git
image_repository: ghcr.io/cogni-dao/red
source_sha: f6ccabfb8f9bd8802bcf8a6e828130f5d9aa939c
candidate_a_branch: deploy/candidate-a-red
preview_branch: deploy/preview-red
production_branch: deploy/production-red
# task.5017 — per-env node-set (deploy ⊆ provisioned). A wizard birth enters all
# NODE_FORMATION_ENVS; this list is the catalog twin so render-node-appset.sh
# renders exactly the births appset.ts generates. Trimming a node's reach is a
# later catalog edit (drop an env here) once its envs are provisioned.
envs: [candidate-a, preview, production]
path_prefix: nodes/red/
node_id: 895147d5-2ad9-4b2b-aeaa-f2669999fdce
