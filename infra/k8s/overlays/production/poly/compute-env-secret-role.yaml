apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: poly-compute-env-secrets-reader
  namespace: cogni-production
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    resourceNames:
      - poly-compute-env-secrets
    verbs:
      - get
