apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: beacon-compute-env-secrets-reader
  namespace: cogni-production
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    resourceNames:
      - beacon-compute-env-secrets
    verbs:
      - get
