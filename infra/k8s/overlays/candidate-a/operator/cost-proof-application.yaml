# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2026 Cogni-DAO
#
# TEMPORARY task.5071 H1 fixture. This child Application is the narrow sanctioned
# path for a disposable XComputeWorkload: candidate flight copies the complete
# operator overlay to deploy/candidate-a-operator, Argo owns the child, and removing
# this file from the final PR tree prunes the child and its paid resource.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cost-proof
  namespace: argocd
  labels:
    cogni.io/proof: task-5071-child-application
  finalizers: [resources-finalizer.argocd.argoproj.io]
spec:
  project: default
  source:
    repoURL: https://github.com/cogni-dao/cogni.git
    targetRevision: deploy/candidate-a-operator
    path: infra/k8s/overlays/candidate-a/operator/cost-proof
  destination:
    server: https://kubernetes.default.svc
    namespace: cogni-candidate-a
  syncPolicy:
    automated: { prune: true, selfHeal: true }
    syncOptions: [ServerSideApply=true]
