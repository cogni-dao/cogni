apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: toks5-compute-env-secrets-reader
  namespace: cogni-preview
rules:
  - apiGroups:
      - ""
    resources:
      - secrets
    resourceNames:
      - toks5-compute-env-secrets
    verbs:
      - get
