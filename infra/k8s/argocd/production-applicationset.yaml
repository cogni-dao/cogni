# SPDX-License-Identifier: LicenseRef-PolyForm-Shield-1.0.0
# SPDX-FileCopyrightText: 2025 Cogni-DAO
#
# Spec: work/items/task.0376 (per-node lane isolation, GR-1 four-generator shape).
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cogni-production
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/production-operator
        files:
          - path: "infra/catalog/operator.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/production-resy
        files:
          - path: "infra/catalog/resy.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/production-scheduler-worker
        files:
          - path: "infra/catalog/scheduler-worker.yaml"
    - git:
        repoURL: https://github.com/cogni-dao/cogni.git
        revision: deploy/production-node-template
        files:
          - path: "infra/catalog/node-template.yaml"
  template:
    metadata:
      name: "production-{{.name}}"
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/cogni-dao/cogni.git
        targetRevision: "deploy/production-{{.name}}"
        path: "infra/k8s/overlays/production/{{.name}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: cogni-production
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
