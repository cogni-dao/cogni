.next
out
dist
build
coverage
node_modules
*.d.ts
pnpm-lock.yaml
package-lock.json
yarn.lock
docs/research/fixtures/*.json
.context/

# Machine-written kustomize overlays (bug.0344 / task.0349).
# Rewritten by scripts/ci/promote-k8s-image.sh on deploy/* branches (flight-preview,
# candidate-flight, promote-and-deploy) and by CI digest seeding on main
# (promote-preview-digest-seed.yml). Their YAML style disagrees with Prettier;
# gating format:check on these paths creates churn. Humans may run Prettier by hand.
infra/k8s/overlays/**/kustomization.yaml

# Byte-exact pnpm-lock test fixtures (node-app-scaffold gens) — must stay raw
nodes/operator/app/src/features/node-app-scaffold/gens/__fixtures__/
